package com.cisc181.core;
import java.util.UUID;

public class Section {
//CourseID (UUID), SemesterID (UUID), SectionID (UUID), RoomID (int).  
	private UUID CourseID;
	private UUID SemesterID;
	private UUID SectionID;
	private int RoomID;
}
